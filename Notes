Things to do:

* make a container that mounts a volume from cinder - can openstack launch a user owned volume on an a project admin's instance? - I'm not modifying keystone!!!!!



Amazon S3 Fuse:

apt-get install -y build-essential libfuse-dev libcurl4-openssl-dev libxml2-dev mime-support automake
wget https://github.com/s3fs-fuse/s3fs-fuse/archive/master.zip
unzip master.zip
rm -f master.zip
cd s3fs-fuse-master
./autogen.sh
./configure
make
make install



